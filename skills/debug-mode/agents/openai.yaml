interface:
  display_name: "Debug Mode"
  short_description: "Prove and fix runtime bugs with temporary logs"
  default_prompt: "Use $debug-mode to diagnose this reproducible runtime bug, fix the confirmed cause, verify it, and clean up."
policy:
  allow_implicit_invocation: true
